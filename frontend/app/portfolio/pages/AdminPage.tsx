"use client";

// Admin page wraps the existing dev-console DevConsole component
// so it appears as the "Top" page in the portfolio layout.
// The /dev-console route still works independently.

export { default } from "@/app/dev-console/page";
